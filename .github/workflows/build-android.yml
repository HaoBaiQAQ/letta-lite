name: 自动适配可用版本（无需手动找链接）
on: [workflow_dispatch]
jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取源码
        uses: actions/checkout@v4

      - name: 安装基础依赖
        run: sudo apt-get update && sudo apt-get install -y make perl wget curl unzip

      - name: 安装NDK r25c（稳定版）
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c
          add-to-path: true

      - name: 自动找NDK和Clang（精准无遍历）
        run: |
          NDK_BUILD=$(which ndk-build)
          [ -z "$NDK_BUILD" ] && { echo "❌ 未找到NDK"; exit 1; }
          NDK_ROOT=$(dirname $(dirname $(dirname $NDK_BUILD)))
          CLANG=$(find $NDK_ROOT -name "aarch64-linux-android*clang" -type f | grep -v "clang++" | head -1)
          [ -z "$CLANG" ] && { echo "❌ 未找到Clang"; exit 1; }
          echo "CLANG=$CLANG" >> $GITHUB_ENV
          echo "AR=$(dirname $CLANG)/llvm-ar" >> $GITHUB_ENV

      - name: 自动尝试1.1.1系列可用版本（核心自动逻辑）
        run: |
          # 1.1.1系列稳定版本列表（按发布时间排序，优先尝试较新的）
          OPENSSL_VERSIONS=("1.1.1t" "1.1.1q" "1.1.1n" "1.1.1k" "1.1.1w" "1.1.1y")
          DOWNLOAD_SUCCESS=0
          SELECTED_VERSION=""

          # 循环尝试每个版本，直到下载成功
          for VERSION in "${OPENSSL_VERSIONS[@]}"; do
            echo "🔍 尝试下载OpenSSL $VERSION..."
            wget --no-check-certificate "https://www.openssl.org/source/old/1.1.1/openssl-$VERSION.tar.gz" -O openssl.tar.gz
            
            # 检查下载是否成功（wget返回0为成功）
            if [ $? -eq 0 ]; then
              echo "✅ 成功下载OpenSSL $VERSION"
              SELECTED_VERSION=$VERSION
              DOWNLOAD_SUCCESS=1
              break
            else
              echo "❌ OpenSSL $VERSION 下载失败，尝试下一个版本..."
              rm -f openssl.tar.gz  # 删除失败的文件
            fi
          done

          # 如果所有版本都失败，退出报错
          if [ $DOWNLOAD_SUCCESS -eq 0 ]; then
            echo "❌ 所有1.1.1系列版本下载失败，请检查网络或稍后重试"
            exit 1
          fi

          # 解压并编译选中的版本
          tar -zxf openssl.tar.gz && cd openssl-$SELECTED_VERSION
          export CC=$CLANG AR=$AR CFLAGS="-D__ANDROID_API__=24 -fPIC" LDFLAGS="-fPIC"
          ./Configure android-arm64 --prefix=$GITHUB_WORKSPACE/openssl no-shared no-zlib no-asm
          make -j2 && make install && cd .. && rm -rf openssl.tar.gz openssl-$SELECTED_VERSION*

      - name: 安装Rust和cargo-ndk
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          target: aarch64-linux-android
      - run: cargo install cargo-ndk --version 3.5.4 --locked

      - name: 编译项目
        run: |
          export OPENSSL_DIR=$GITHUB_WORKSPACE/openssl PKG_CONFIG=false
          export RUSTFLAGS="-C linker=$CLANG -C link-arg=-fPIC"
          cargo ndk -t aarch64-linux-android -o target/android build --profile mobile

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: letta-lite-android
          path: target/android/*.so
